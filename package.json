{
  "name": "design-pattern-typescript",
  "version": "1.0.0",
  "description": "design pattern typescript",
  "main": "index.js",
  "scripts": {
    "start": "npm run build && node dist/index.js",
    "build": "npx tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "EnochGao",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^20.2.0",
    "typescript": "^5.3.3"
  }
}
